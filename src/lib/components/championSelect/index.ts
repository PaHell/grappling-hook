export enum ChampionSelectPhase {
      Ban,
      Pick
}

export enum BanState {
      Idle,
      Active,
      Locked
}

export enum PickState {
      Idle,
      Active,
      Locked
}

export enum PlayerRole {
      Top,
      Jungle,
      Mid,
      Bot,
      Support
}